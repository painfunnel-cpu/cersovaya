# Supabase

The browser is configured with the project URL and publishable key.

Do NOT put `sb_secret_...` or the legacy `service_role` JWT into this frontend, GitHub, or public Vercel variables. Those are privileged server-side secrets.

Project URL: https://rquwkqcodjemwmlfrbjj.supabase.co
